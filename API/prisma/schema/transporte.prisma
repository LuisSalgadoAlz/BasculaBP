model Transporte {
    id Int @id @default(autoincrement())
    nombre String
    descripcion String?
    estado String
    idPlaca Int
    
    /* Relaciones con boleta */
    boletaTransporte Boleta[]
    motoristaBoleta Placas []
}