BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Tolva] DROP CONSTRAINT [Tolva_idBoleta_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
