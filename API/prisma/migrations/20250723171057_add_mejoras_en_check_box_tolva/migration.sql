BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Tolva] ADD [Sello1] BIT,
[Sello2] BIT,
[Sello3] BIT,
[Sello4] BIT,
[Sello5] BIT,
[Sello6] BIT,
[observacionMarchamos] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
