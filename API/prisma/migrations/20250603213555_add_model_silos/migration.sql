BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Silos] (
    [id] INT NOT NULL IDENTITY(1,1),
    [nombre] NVARCHAR(1000) NOT NULL,
    [capacidad] FLOAT(53),
    [estado] BIT,
    CONSTRAINT [Silos_pkey] PRIMARY KEY CLUSTERED ([id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
